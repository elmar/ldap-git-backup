#!/usr/bin/env perl
@PERL_PREAMBLE@
use English qw( -no_match_vars );

use Test::More;

ok(system('perl', "@builddir@/ldap-git-backup", '--help') == 0, 'option --help should be allowed');

done_testing();
